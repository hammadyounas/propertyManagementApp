/**
 * Row definition for read-only sales team detail lists (label + data key).
 *
 * @typedef {Object} SalesTeamDetailField
 * @property {string} label Human-readable label shown in the description list.
 * @property {string} valueKey Key on `salesteamData` used to read the value.
 */

export {};
