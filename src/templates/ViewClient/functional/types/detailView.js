/**
 * Row definition for read-only client detail lists (label + data key).
 *
 * @typedef {Object} ClientDetailField
 * @property {string} label Human-readable label shown in the description list.
 * @property {string} valueKey Key on `clientData` used to read the value.
 */

export {};
